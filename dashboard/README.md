# LaunchDesk

A Protopage-style personal dashboard/start page prototype built as a dependency-free static website.

## Run

```bash
npm run serve
```

Then open `http://localhost:4173`.

Dashboard state is saved in `localStorage`. Use the settings panel to export or import JSON.
